<h2>로그인</h2>

<br />

<%= form_for(resource, :as => resource_name, :url => session_path(resource_name), :html => { :class => 'form-inline', :'accept-charset' => 'utf-8' } ) do |f| %>

  <p>
  <%= link_to user_omniauth_authorize_path(:facebook), :class => "zocial facebook" do %>
    <span>Facebook</span>
  <% end %>
  이나 
  <a title="Enter using twitter account" href="#" class="zocial twitter "><span>Twitter</span></a> 로 접속하세요. 아니면 이메일과 패스워드를 입력해주세요.</p>

  <br />

  <div class="input-prepend">
    <span class="add-on"><i class="icon-envelope"></i></span><%= f.email_field :email, :class => 'span2', :placeholder => "이메일" %>
  </div>

  <div class="input-prepend">
    <span class="add-on"><i class="icon-key"></i></span><%= f.password_field :password, :class => 'span2', :placeholder => "패스워드" %>
  </div>

  <%= f.submit "로그인", :class => 'btn btn-primary', :title => '로그인' %>

  <% if devise_mapping.rememberable? -%>
    <%= f.check_box :remember_me, :style => "visibility:hidden;", :checked => true %>
  <% end %>

<% end %>

<%= render "devise/shared/links" %>
